class Video {
  constructor(title, author, coverUrl, views, date, duration, authorCover) {
    this.title = title;
    this.author = author;
    this.coverUrl = coverUrl;
    this.views = views;
    this.date = date;
    this.duration = duration;
    this.authorCover = authorCover;
  }
}

export default Video;
